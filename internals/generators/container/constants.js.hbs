/*
*
* {{ properCase name }} constants
*
*/

import { generateActionTypes } from '../../utils/generic-saga'
const root = 'app/{{ upperCase name }}/'

export default {
defaultAction: generateActionTypes(root, 'DEFAULT_ACTION'),
}